# Convert binary file into RGB images
## Usage
0. install python dependencies

```bash
python3 -m pip install requirements.txt
```

1. decode binary files
```bash
python3 bin2img.py [/path/to/data/root]
```
